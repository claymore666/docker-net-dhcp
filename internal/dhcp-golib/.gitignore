# local session scaffolding, never committed
.claude/
CLAUDE.md
# build output
/bin/
*.test

# The oracle stamp: per clone, written only by an accepted oracle pass, and
# bound to the root it was written for. It must never travel with a commit —
# a stamp in the tree is a skip somebody else inherits.
.verify-oracle-stamp
