{
    "description": "Docker DHCP networking (host-bridge or macvlan attachment)",
    "interface": {
        "socket": "net-dhcp.sock",
        "types": [
            "docker.networkdriver/1.0"
        ]
    },
    "entrypoint": [
        "/usr/sbin/net-dhcp",
        "-logfile",
        "/var/log/net-dhcp.log"
    ],
    "env": [
        {
            "description": "Log level",
            "name": "LOG_LEVEL",
            "value": "info",
            "settable": [
                "value"
            ]
        },
        {
            "description": "Timeout while awaiting container/sandbox readiness",
            "name": "AWAIT_TIMEOUT",
            "value": "10s",
            "settable": [
                "value"
            ]
        },
        {
            "description": "Directory where per-network options are persisted",
            "name": "STATE_DIR",
            "value": "/var/lib/net-dhcp",
            "settable": [
                "value"
            ]
        },
        {
            "description": "Optional TCP address for the Prometheus /metrics endpoint (e.g. 127.0.0.1:9099); empty means no TCP listener",
            "name": "METRICS_ADDR",
            "value": "",
            "settable": [
                "value"
            ]
        },
        {
            "description": "Docker API endpoint; empty means the mounted /run/docker.sock. Set it to a read-only socket proxy: anything but GET and HEAD is refused before it is sent",
            "name": "DOCKER_HOST",
            "value": "",
            "settable": [
                "value"
            ]
        }
    ],
    "workdir": "/",
    "network": {
        "type": "host"
    },
    "pidhost": true,
    "mounts": [
        {
            "source": "/var/run/docker.sock",
            "destination": "/run/docker.sock",
            "type": "bind",
            "options": [
                "bind"
            ]
        },
        {
            "source": "/var/lib/net-dhcp",
            "destination": "/var/lib/net-dhcp",
            "type": "bind",
            "options": [
                "rbind",
                "rw"
            ]
        },
        {
            "source": "/var/run/docker",
            "destination": "/var/run/docker",
            "type": "bind",
            "options": [
                "rbind",
                "ro"
            ]
        }
    ],
    "linux": {
        "capabilities": [
            "CAP_NET_ADMIN",
            "CAP_NET_RAW",
            "CAP_SYS_ADMIN",
            "CAP_SYS_PTRACE"
        ]
    }
}
